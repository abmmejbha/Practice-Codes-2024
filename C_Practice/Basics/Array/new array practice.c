#include<stdio.h>
int main()
{
    int index;
    printf("Enter the value of index:");
    for(index=0; index<5; index++)
    {
        printf("Array[%d]=",index);
        scanf("%d",&numbers[index]);
    }
return 0;
}
